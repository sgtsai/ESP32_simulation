#include "rsa_baseline.h"

/*
 * Instructor-only reference answer template.
 *
 * To use privately:
 *   1. Copy this file to main/rsa_refans.c.
 *   2. Implement rsa_encrypt_u64_refans().
 *   3. Keep main/rsa_refans.c uncommitted; it is ignored by .gitignore.
 */

uint64_t rsa_encrypt_u64_refans(uint64_t plaintext, uint64_t exponent, uint64_t modulus)
{
    /*
     * Instructor TODO:
     * Put the private optimized answer here. Do not distribute the completed
     * main/rsa_refans.c file to students.
     */
    return rsa_encrypt_u64_baseline(plaintext, exponent, modulus);
}
